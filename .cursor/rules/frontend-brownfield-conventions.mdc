---
description: 前端棕地项目规则 — 约束 AI 在 Vue/React/TS/JS 前端的实现行为
globs:
  - "**/*.vue"
  - "**/*.jsx"
  - "**/*.tsx"
  - "**/*.ts"
  - "**/*.js"
  - "**/*.scss"
  - "**/*.less"
  - "**/*.css"
---

# 前端棕地规则

## 路由与页面组织

1. 新增页面必须沿用现有路由配置方式和文件组织结构，不允许引入新的路由框架或目录规范。
2. 页面入口必须挂载到现有菜单系统，不允许脱离现有导航体系。
3. 页面组件命名和目录位置必须与现有页面保持一致风格。

## 组件复用

1. 列表页必须复用现有列表页 layout/template，不允许重做页面骨架。
2. 查询区必须复用现有查询表单组件和重置逻辑。
3. 新增/修改表单优先复用现有 Dialog / Drawer 组件模式。
4. 表格必须复用现有 Table 组件，沿用现有列配置和分页组件。
5. 导出按钮必须复用现有导出按钮组件和 loading 反馈模式。

## API 封装

1. 接口调用必须通过项目已有的 API 封装层（如 axios 实例 + 统一拦截器），不允许直接裸写 `fetch` 或新建 axios 实例。
2. API 文件组织方式必须与现有模块保持一致。

## 表单校验

1. 表单校验必须复用现有校验规则模式和错误提示方式。
2. 不允许引入新的校验库替代现有方案。

## 样式

1. 必须沿用现有设计系统和组件库，不允许替换为新的 UI 框架。
2. 页面样式必须与现有系统保持视觉一致。
3. 不允许引入新的 CSS 框架或全局样式覆盖。

## 兼容性

1. 新增页面和组件不允许修改现有公共组件的行为。
2. 不允许修改现有状态管理的全局状态结构。
3. 如果需要复用现有组件但发现该组件能力不足，必须先确认影响面再决定是否扩展，不允许直接 fork 一份。

## 测试

1. 新增页面应包含基本的组件渲染和交互验证。
2. 必须验证旧页面在新改动后仍能正常工作（至少手动验证关键页面）。
3. 浏览器控制台不允许存在由新改动引入的错误或警告。

