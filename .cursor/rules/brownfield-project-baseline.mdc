---
description: 棕地项目级统一规则基线 — 约束 AI 在历史项目中的整体行为
globs:
alwaysApply: true
---

# 棕地项目基线规则

## 核心纪律

1. 当前项目是历史项目（棕地项目），不是新项目。所有设计和实现必须在理解现状后增量扩展，不允许按绿地项目思路重做。
2. 在当前 change 范围内，以 OpenSpec artifacts（proposal/spec/design/tasks）作为唯一规格真相源。
3. 不允许在没有验证证据的情况下宣称"功能完成"或"具备发布评审条件"。
4. 对不确定的历史逻辑、字段语义、主数据来源，必须显式标记为"待确认"，不允许猜测。

## 流程约束

1. 先理解现状，再做设计，再做实现。不允许跳过探索直接进入编码。
2. 发现需求遗漏时，先回写 OpenSpec artifacts，再继续开发。不允许代码跑在 spec 前面。
3. 所有完成声明必须有结构化验证证据（command、environment、result、conclusion、residual risk）。

## 兼容性约束

1. 不允许修改现有接口的请求/响应结构，除非 spec 明确要求。
2. 不允许无关重构。每次改动只承载当前 change 范围内的变更。
3. 必须同时验证新功能和旧链路未被破坏。

## 真相源优先级

1. 当前已确认并更新的 OpenSpec artifacts
2. 与之同步后的实施计划
3. 当前实现与验证证据
4. AI 的建议和草案仅作为参考，不作为最终事实依据

